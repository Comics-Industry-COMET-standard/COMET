// @ts-check
// The sidebar is authored explicitly (rather than fully autogenerated) because
// the picklist pages are numerous and generated; we point the category at the
// generated directory so new picklists appear automatically.

/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  reference: [
    'intro',
    'data-dictionary',
    {
      type: 'category',
      label: 'Fields by tier',
      collapsed: false,
      items: ['fields/fields-essential', 'fields/fields-core', 'fields/fields-extended'],
    },
    {
      type: 'category',
      label: 'Picklists',
      collapsed: true,
      // docs/picklists/index.md is auto-detected as this category's index page;
      // every other generated docs/picklists/*.md is listed under it.
      items: [{ type: 'autogenerated', dirName: 'picklists' }],
    },
    'implementation-guide',
    'white-paper',
    'changelog',
  ],
};

module.exports = sidebars;
